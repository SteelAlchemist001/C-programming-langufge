Console.Clear();
int[] array1 = new int[5];
int[] array2 = {1, 2, 3, 4, 5};
int length = array2.Length;
int index = 0;  while (index < length)
       { 
            if (array2[index] %2 == 0)
         {  
             FillArray(array1);
            } 
             else
               {  
                   (array2[index] %2 != 0);
              }    
              index++;
                }  
     return position;
               } 
FillArray(array1);
PrintArray(array1);
        
